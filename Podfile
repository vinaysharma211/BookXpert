# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BookXpert' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BookXpert
  pod 'FirebaseCore', '10.3.0'
  pod 'FirebaseAuth', '10.3.0'
  pod 'FirebaseMessaging', '10.3.0'
  pod 'GoogleSignIn', '7.1.0' # Keep this if it’s working for you

end
